---
// 4-up stats grid wrapper. Collapses to 2 columns on small screens.
interface Props {
  class?: string;
}
const { class: extra = '' } = Astro.props;
---

<div class:list={['grid grid-cols-2 md:grid-cols-4', extra]}>
  <slot />
</div>

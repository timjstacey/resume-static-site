# Cloudflare Web Analytics beacon token (Manual setup). When set at build time,
# Base.astro injects the cookieless beacon. Leave unset locally; set it only on
# the Cloudflare Pages *Production* environment so preview/dev don't report.
CF_BEACON_TOKEN=
